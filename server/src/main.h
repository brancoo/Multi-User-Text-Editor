#include "estruturas.h"

extern Editor editor;
extern int max_users;

int getMAX_USERS(int n);
bool find_username(char *username, char *filename);
void verify_env_var();
int main(int argc, char *argv[]);
